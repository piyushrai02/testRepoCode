import React from 'react';

const SearchBar1 =  = {
  console.log('test')
  return (
    <div className="search-bar">
      <input
        type="text"
        placeholder="Search for items check..."
        value={value}
        onChange={onChange}
        className="todo-input search-input"
        style={{ marginBottom: '10px', borderRadius: '4px', width: '100%' }}
      />
    </div>
  );
};

export default SearchBar1;
